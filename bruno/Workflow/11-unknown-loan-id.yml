info:
  name: 11 unknown loan id is 404
  type: http
  seq: 11
  tags:
    - Workflow

http:
  method: POST
  url: "{{baseUrl}}/advisor/loan-requests/REQ209912310000/decision"
  body:
    type: json
    data: |-
      {
        "decision": "approved"
      }

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"
    - expression: res.body.error.code
      operator: eq
      value: "NOT_FOUND"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: false
  maxRedirects: 0

docs: |-
  Well-formed per isLoanId, but no such row.
